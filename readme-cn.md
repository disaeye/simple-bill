# Simple Bill - 支出记账本

一个现代化、轻量级的支出记账 Web 应用，使用原生 JavaScript 构建。

![版本](https://img.shields.io/badge/版本-1.0.0-blue)
![许可证](https://img.shields.io/badge/许可证-MIT-green)

## 功能特点

- 📊 **可视化分析** - 交互式图表展示支出趋势和分类占比
- 🔍 **智能筛选** - 支持按时间维度（月/季度/年/周）、出账日、分类筛选
- 📱 **移动端适配** - 在桌面端、平板、手机上都能获得完美体验
- 📈 **趋势分析** - 环比分析，清晰展示收支变化
- 🎯 **图表下钻** - 点击分类图表查看二级分类详情
- 💾 **数据导入导出** - 支持 JSON 格式导入导出
- 🌐 **静态部署** - 无需后端，部署到任何静态托管服务

## 快速开始

### 在线演示

访问：[https://simple-bill.vercel.app](https://simple-bill.vercel.app)

### 本地运行

```bash
# 克隆仓库
git clone https://github.com/disaeye/simple-bill.git
cd simple-bill

# 启动本地服务器
# 方式一：Python
python -m http.server 8080

# 方式二：Node.js
npx serve .

# 浏览器打开
# http://localhost:8080
```

### 数据格式

在 `data/` 目录下放置 `expenses.json` 文件：

```json
{
  "expenses": [
    {
      "id": "唯一标识符",
      "date": "2026-01-15",
      "amount": -150.00,
      "type": "expense",
      "category1": "餐饮",
      "category2": "午餐",
      "remark": "工作餐"
    }
  ]
}
```

**数据字段说明：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | string | 是 | 唯一标识符 |
| date | string | 是 | 日期，格式 YYYY-MM-DD |
| amount | number | 是 | 金额，收入为正，支出为负 |
| type | string | 是 | 类型：expense/refund/income |
| category1 | string | 是 | 一级分类 |
| category2 | string | 否 | 二级分类 |
| remark | string | 否 | 备注 |

## 部署

### Vercel（推荐）

```bash
# 安装 Vercel CLI
npm i -g vercel

# 部署
vercel --prod
```

或将 GitHub 仓库连接到 Vercel，实现自动部署。

### Netlify

1. 将 GitHub 仓库连接到 Netlify
2. 构建命令：（留空）
3. 发布目录：.
4. 部署！

### GitHub Pages

```bash
# 在仓库设置中启用 GitHub Pages
# 来源选择 main 分支
```

## 自定义

### 分类配置

编辑 `data/categories.json`：

```json
{
  "categories": {
    "餐饮": ["早餐", "午餐", "晚餐", "外卖"],
    "交通": ["公交", "地铁", "打车"]
  },
  "icons": {
    "餐饮": "🍜",
    "交通": "🚗"
  },
  "colors": {
    "餐饮": "#E17055",
    "交通": "#00B894"
  }
}
```

### 样式修改

编辑 `css/style.css` 自定义颜色、字体和布局。

### 二次开发

项目使用 IIFE 模块模式：

| 模块 | 文件 | 功能 |
|------|------|------|
| DataModule | js/data.js | 数据处理、筛选、统计 |
| FilterModule | js/filter.js | 筛选状态管理 |
| UIModule | js/ui.js | UI 渲染、DOM 操作 |
| ChartModule | js/chart.js | ECharts 图表集成 |
| AppModule | js/app.js | 主应用逻辑 |

## 项目结构

```
simple-bill/
├── index.html          # 主 HTML 文件
├── css/
│   └── style.css      # 样式文件
├── js/
│   ├── data.js        # 数据处理模块
│   ├── filter.js      # 筛选状态模块
│   ├── ui.js         # UI 组件模块
│   ├── chart.js      # 图表模块
│   └── app.js       # 主应用
└── data/
    ├── categories.json # 分类配置
    └── expenses.json   # 支出数据
```

## 技术栈

- **原生 JavaScript** (ES6+) - 无框架依赖
- **ECharts 5** - 交互式图表
- **CSS3** - 使用 CSS 变量管理样式
- **Vercel** - 部署平台

## 性能

- JS 约 15KB（压缩后）
- CSS 约 5KB
- 无需构建步骤
- 外部库使用 CDN

## 二次开发指南

### 添加新筛选条件

1. 在 `index.html` 添加筛选 UI
2. 在 `UIModule.initElements()` 注册元素
3. 在 `FilterModule` 添加事件监听
4. 在 `FilterModule.applyFilters()` 使用

### 添加新图表

1. 在 `index.html` 添加图表容器
2. 在 `ChartModule.initCharts()` 初始化
3. 在 `ChartModule` 添加更新函数
4. 在 `AppModule.refreshDisplay()` 调用

### 添加新数据字段

1. 在 `expenses.json` 添加字段
2. 如需要，更新 `DataModule` 函数
3. 在 `UIModule.renderExpenseList()` 添加列

## 许可证

MIT 许可证 - 可免费用于个人和商业项目。
